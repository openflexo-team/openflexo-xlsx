// We test here the basic use of Excel technology adapter, with a xlsx file in loading mode

// Perform some initialization, display resources and the context
service ResourceCenterService status;
resources;
context;

// Load virtual model instance
vmi = load -r ["http://www.openflexo.org/excel-test/TestPersonListing2.fml.rt"];
context;
assert vmi != null;

log "vmi="+vmi;

log "instances: "+vmi.getFlexoConceptInstances();

persons = vmi.getAllPersons();
log "persons="+persons;

assert persons.size==6;

jeanDupont = persons.get(0);
assert jeanDupont.name == "Jean Dupont";

alainTerrieur = persons.get(5);
assert alainTerrieur.name == "Alain Terrieur";
